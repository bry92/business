[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-gateway"
version = "0.1.0"
description = "Prototype AI-native tool-calling gateway for agent workloads."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
